<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Estilo específico para o título */
        .title {
            text-align: center;
            font-family: 'Arial', sans-serif; /* Use a fonte desejada aqui */
            font-weight: 600; /* Ajuste o peso da fonte conforme necessário */
            color: #333; /* Cor do texto */
            background-color: rgb(199, 199, 199);
            padding: 20px;
            margin: 0;
        }
    </style>
</head>
<body>
    {{{body}}}
</body>
</html>
